// 按钮类型
export enum EButtonType {
  default = 'default',
  primary = 'primary',
  success = 'success',
  warning = 'warning',
  danger = 'danger',
  info = 'info',
  text = 'text',
}

export enum EButtonSize {
  medium = 'medium',
  small = 'small',
  mini = 'mini',
}

export enum ENativeButtonType {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}

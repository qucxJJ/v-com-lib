export enum EMessageType {
  success = 'success',
  warning = 'warning',
  error = 'error',
  info = 'info'
}
export enum EMessageTime {
  default = 1000
}
export enum EMessageContent {
  default = '这是提示内容'
}

export enum EMessageShow {
  default = 'false'
}